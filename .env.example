# Copy this file to .env and fill in real values for local overrides.
# .env is already git-ignored, so real credentials never get committed.

DB_URL=jdbc:postgresql://localhost:5432/studentdb
DB_USERNAME=studentapp
DB_PASSWORD=studentpass
PORT=8080
